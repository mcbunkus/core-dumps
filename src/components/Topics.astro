---
interface Props {
    topics: string[];
    class?: string;
}

const { topics, class: className } = Astro.props;
---

<div class={`flex gap-2 brightness-75 ${className}`}>
    {topics.map((topic) => <a href={`/topics/${topic}`}>#{topic}</a>)}
</div>
